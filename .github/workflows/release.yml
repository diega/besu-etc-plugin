name: Release

on:
  push:
    branches: [main]
    paths: ['.github/workflows/release.yml']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  plan:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.p.outputs.matrix }}
      tags: ${{ steps.p.outputs.tags }}
    steps:
      - id: p
        run: |
          cat > /tmp/releases.yml <<'YAML'
          stable:
            besu_branch: 26.4.0-RC1-plugin-extensions
            besu_version: 26.4.0-RC1
          experimental:
            - name: on-10220
              besu_branch: plugin-extensions-on-10220
              besu_version: 26.3.0-SNAPSHOT
            - name: on-10220-plus-storageroot-fix-and-10235
              besu_branch: plugin-extensions-on-10220-plus-storageroot-fix-and-10235
              besu_version: 26.3.0-SNAPSHOT
          YAML
          python3 <<'PY'
          import yaml, json, os
          cfg = yaml.safe_load(open('/tmp/releases.yml'))
          items = []
          s = cfg['stable']
          items.append({
            'name': 'stable',
            'experimental': False,
            'besu_branch': s['besu_branch'],
            'besu_version': s['besu_version'],
            'tag': f"{s['besu_version']}-etc",
          })
          for e in cfg.get('experimental') or []:
            items.append({
              'name': e['name'],
              'experimental': True,
              'besu_branch': e['besu_branch'],
              'besu_version': e['besu_version'],
              'tag': f"{e['besu_version']}-etc-{e['name']}",
            })
          tags = [it['tag'] for it in items]
          with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
            f.write(f"matrix={json.dumps({'include': items})}\n")
            f.write(f"tags={json.dumps(tags)}\n")
          PY

  build:
    needs: plan
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.plan.outputs.matrix) }}
    steps:
      - name: Checkout plugin
        uses: actions/checkout@v4

      - name: Checkout Besu fork
        uses: actions/checkout@v4
        with:
          repository: diega/besu
          ref: ${{ matrix.besu_branch }}
          path: besu

      - name: Compute effective version
        id: ver
        run: |
          if [ "${{ matrix.experimental }}" = "true" ]; then
            SHA=$(git -C besu rev-parse --short=7 HEAD)
            EFFECTIVE="${{ matrix.besu_version }}-${SHA}"
            ARTIFACT="${{ matrix.tag }}-${SHA}"
          else
            EFFECTIVE="${{ matrix.besu_version }}"
            ARTIFACT="${{ matrix.tag }}"
          fi
          echo "effective=${EFFECTIVE}" >> $GITHUB_OUTPUT
          echo "artifact=${ARTIFACT}" >> $GITHUB_OUTPUT

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Publish Besu to mavenLocal
        working-directory: besu
        run: ./gradlew publishToMavenLocal -Pversion=${{ steps.ver.outputs.effective }} -x test -x javadoc

      - name: Build plugin
        run: ./gradlew build -PbesuVersion=${{ steps.ver.outputs.effective }} -PbuildVersion=${{ steps.ver.outputs.effective }}

      - name: Build Besu distribution
        working-directory: besu
        run: ./gradlew installDist -Pversion=${{ steps.ver.outputs.effective }} -x test -x autocomplete

      - name: Assemble release
        run: |
          ARTIFACT="${{ steps.ver.outputs.artifact }}"
          mkdir -p besu/build/install/besu/plugins
          cp build/libs/besu-etc-plugin-*.jar besu/build/install/besu/plugins/
          cd besu/build/install
          tar czf $GITHUB_WORKSPACE/besu-etc-${ARTIFACT}.tar.gz besu/
          zip -r $GITHUB_WORKSPACE/besu-etc-${ARTIFACT}.zip besu/
          cd $GITHUB_WORKSPACE
          sha256sum besu-etc-${ARTIFACT}.tar.gz > besu-etc-${ARTIFACT}.tar.gz.sha256
          sha256sum besu-etc-${ARTIFACT}.zip > besu-etc-${ARTIFACT}.zip.sha256
          cp build/libs/besu-etc-plugin-*.jar .
          sha256sum besu-etc-plugin-*.jar > besu-etc-plugin-${ARTIFACT}.jar.sha256

      - name: Clean stale assets from release
        if: matrix.experimental
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release view "${{ matrix.tag }}" -R "${{ github.repository }}" --json assets --jq '.assets[].name' 2>/dev/null \
            | xargs -r -I {} gh release delete-asset "${{ matrix.tag }}" {} -R "${{ github.repository }}" --yes

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ matrix.tag }}
          name: ${{ matrix.tag }}
          prerelease: true
          files: |
            besu-etc-${{ steps.ver.outputs.artifact }}.tar.gz
            besu-etc-${{ steps.ver.outputs.artifact }}.tar.gz.sha256
            besu-etc-${{ steps.ver.outputs.artifact }}.zip
            besu-etc-${{ steps.ver.outputs.artifact }}.zip.sha256
            besu-etc-plugin-*.jar
            besu-etc-plugin-${{ steps.ver.outputs.artifact }}.jar.sha256

  reconcile:
    needs: [plan, build]
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      DECLARED: ${{ needs.plan.outputs.tags }}
      REPO: ${{ github.repository }}
    steps:
      - name: Delete releases not declared in plan
        run: |
          declared=$(echo "$DECLARED" | python3 -c 'import json,sys; print("\n".join(json.load(sys.stdin)))')
          existing=$(gh release list -R "$REPO" --json tagName --jq '.[].tagName')
          for tag in $existing; do
            case "$tag" in
              *-etc|*-etc-*) ;;
              *) continue ;;
            esac
            if ! echo "$declared" | grep -qx "$tag"; then
              echo "Deleting stale release: $tag"
              gh release delete -R "$REPO" "$tag" --cleanup-tag --yes
            fi
          done
